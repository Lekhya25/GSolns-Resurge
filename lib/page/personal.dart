import 'package:flutter/material.dart';

class PersonalDatailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Personal Details'),
          centerTitle: true,
          backgroundColor: Colors.red,
        ),
      );
}