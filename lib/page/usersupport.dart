import 'package:flutter/material.dart';

class UserSupportPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('User Support and Counselling'),
          centerTitle: true,
          backgroundColor: Colors.red,
        ),
      );
}